//1.Crea una función que calcule el índice de masa corporal (IMC) de una persona a partir de su altura en metros y peso en kilogramos, que se recibirán como parámetros.
/*
function imc(peso,altura){
    let masa = peso / (altura*altura);
    return masa;
}

//2.Crea una función que calcule el valor del factorial de un número pasado como parámetro.

function calcularFactorial(num) {
    if (num === 0 || num === 1) {
      return 1;
    } else {
      return num * calcularFactorial(num - 1);
    }
  }

  let num = 5;
  let resultado = calcularFactorial(num);
  console.log(`El factorial de ${num} es ${resultado}`);

  //3.Crea una función que convierta un valor en dólares, pasado como parámetro, y devuelva el valor equivalente en reales. Para esto, considera la cotización del dólar igual a R$4,80.
function conversion(dinero){
    let realUsd = dinero * 4.80;
    return realUsd;
}
//4.Crea una función que muestre en pantalla el área y el perímetro de una sala rectangular, utilizando la altura y la anchura que se proporcionarán como parámetros.
function terreno(base, altura){
    let area = base * altura;
    let perimetro = 2 * (base + altura);
    console.log(`El área es: ${area} y el perímetro es: ${perimetro}`);
}
//5.Crea una función que muestre en pantalla el área y el perímetro de una sala circular, utilizando su radio que se proporcionará como parámetro. Considera Pi = 3,14.
function ciculo(radio){
    const pi = 3.1416;
    let areaDelCirculo = pi * Math.pow(radio , 2);
    let perimetroDelCirculo = 2 * pi * radio;

    console.log(`El area del ciculo es ${areaDelCirculo} y su perimetro es ${perimetroDelCirculo}`);
}

//6.Crea una función que muestre en pantalla la tabla de multiplicar de un número dado como parámetro.
function tabla(numero){
    for (var i = 1; i <= 10; i++) {
        var resultado = numero * i;
        console.log(numero + " x " + i + " = " + resultado);
      }
}*/